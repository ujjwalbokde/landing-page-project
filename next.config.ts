import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // module.exports = {
    images: {
      domains: ['aceternity.com', 'another-domain.com', 'yet-another-domain.com','images.unsplash.com'],
    },
  // };
};

export default nextConfig;
