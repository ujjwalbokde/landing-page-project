/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['aceternity.com','assets.aceternity.com', 'another-domain.com', 'yet-another-domain.com', 'images.unsplash.com'],
  },
};

export default nextConfig;
